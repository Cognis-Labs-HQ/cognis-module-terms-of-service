# <Document title>

Begin with an informative, descriptive statement that explains what the documented function, component, route, capability, or process accomplishes and why a caller uses it. Do not put an Overview heading before this statement.

## <Usage examples>

Show concrete imports, capability lookups, route calls, configuration, and expected results for new code. Examples must use the owning component's public contract and must not import another component's internals.

## <Technical specification>

Document every aspect of the contract after the usage section: inputs, outputs, lifecycle, validation, authorization, errors, side effects, persistence, configuration, extension points, routes, security boundaries, cleanup, and operational constraints. Add focused H3 subsections beneath this heading without introducing another H2 before it.
